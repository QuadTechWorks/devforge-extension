version: 2
updates:
  # The project itself has no runtime dependencies (vanilla JS, no build).
  # This only keeps the GitHub Actions used by CI up to date.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
